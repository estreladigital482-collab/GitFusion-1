services:
  gitfusion:
    build: .
    container_name: gitfusion
    ports:
      - "3737:3737"
    environment:
      - PORT=3737
      - WORKSPACE_DIR=/app/workspaces
    volumes:
      - gitfusion-workspaces:/app/workspaces
    restart: unless-stopped
volumes:
  gitfusion-workspaces:
