services:
  gitfusion:
    build: .
    container_name: gitfusion
    ports:
      - "3737:3737"
    volumes:
      - ./data:/app/data
      - ./workspaces:/app/workspaces
    environment:
      - PORT=3737
